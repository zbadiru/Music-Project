<h1><%= @song.title%> </h1>
<h2>By: <%= link_to  "#{@song.artist.name}", artist_path(@song.artist.id) %> </h2>
<h2>In this genre: <%= link_to  "#{@song.genre.title}", artist_path(@song.genre.id) %> </h2>

<%= link_to  "Back", songs_path %><br>
<%= link_to  "Edit song?", edit_song_path %><br>
<%= link_to("Delete song?", song_path(@song.id), method: :delete, data: {confirm: "R u sure u Wanna delete this song?"}) %><br>
